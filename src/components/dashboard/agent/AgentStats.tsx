import React from 'react'
import type { Agent } from '@/types'

interface AgentStatsProps {
  agent: Agent
}

export function AgentStats({ agent }: AgentStatsProps) {
  return (
    <div>
      {/* Implement agent stats */}
    </div>
  )
} 