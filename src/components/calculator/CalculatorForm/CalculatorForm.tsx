import React from 'react'
import type { CalculatorFormProps } from './types'

export function CalculatorForm({ siteId, formId, initialData, onFormUpdate, onDeleteForm }: CalculatorFormProps) {
  return (
    <div>
      {/* Implement calculator form */}
    </div>
  )
} 